#include <bits/stdc++.h>
using namespace std;

//Prim's Algorithm is used to find to Minimum Spanning Tree
int main() {
    int n; 
    cin >> n;
    vector<vector<int>> adjList(n);
    for(int i=0; i<n; i++){
        
    }
    return 0;
}